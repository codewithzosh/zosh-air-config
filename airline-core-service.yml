server:
  port: 5002

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/yt_airline_db
    username: root
    password: $7090Ashok
  jpa:
    hibernate:
      ddl-auto: update


eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true