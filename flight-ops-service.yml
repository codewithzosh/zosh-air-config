server:
  port: 5011

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/yt_airline_flight_db
    username: root
    password: $7090Ashok

  kafka:
    bootstrap-servers: localhost:9092


eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true